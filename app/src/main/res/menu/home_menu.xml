<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/btn_toolbar_home_done"
        android:icon="@drawable/done_white_24dp"
        android:title="@string/log_session"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/btn_clear_drink_list"
        android:title="@string/clear_drink_list"
        app:showAsAction="never"/>

    <item
        android:id="@+id/btn_disclaimer"
        android:title="@string/disclaimer"
        app:showAsAction="never"/>
</menu>