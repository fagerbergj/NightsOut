<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/btn_clear_all_logs"
        android:title="@string/clear_all_logs"
        app:showAsAction="never" />
    <item
        android:id="@+id/btn_clear_selected_day_log"
        android:title="@string/clear_selected_day_logs"
        app:showAsAction="never" />

    <item
        android:id="@+id/btn_move_selected_log"
        android:title="@string/move_selected_log"
        app:showAsAction="never"/>

</menu>